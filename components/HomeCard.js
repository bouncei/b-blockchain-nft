import React from 'react';
import { sanityClient } from '../sanity';

const HomeCard = (({ profileImage, title, description }) =>{
    return (
        <div></div>
    )
})




export default HomeCard